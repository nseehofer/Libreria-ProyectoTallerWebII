generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

model Categoria {
  id     Int     @id(map: "PK__Categori__3213E83F3EB24BCE") @default(autoincrement())
  nombre String  @db.NVarChar(100)
  Libro  Libro[]
}

model Libro {
  id           Int       @id(map: "PK__Libro__3213E83FEF943992") @default(autoincrement())
  nombre       String    @db.NVarChar(255)
  descripcion  String?   @db.NVarChar(Max)
  precio       Decimal   @db.Decimal(10, 2)
  autor        String    @db.NVarChar(255)
  img_src      String    @map("img-src") @db.NVarChar(255)
  id_categoria Int
  Categoria    Categoria @relation(fields: [id_categoria], references: [id], map: "fk_libro_categoria")
}

model Usuario {
  id         Int     @id(map: "PK__Usuario__3213E83F0366B15E") @default(autoincrement())
  email      String  @unique(map: "UQ__Usuario__AB6E61648FA78CC9") @db.NVarChar(255)
  contrase_a String  @map("contraseña") @db.NVarChar(255)
  nombre     String  @db.NVarChar(100)
  apellido   String  @db.NVarChar(100)
  direccion  String? @db.NVarChar(255)
}